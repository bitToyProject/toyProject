import React from "react";
import PropTypes from "prop-types";

const EmailInput = () => {
  return (
    <>
      <label>이메일</label>
      <input type="text" />
    </>
  );
};

export default EmailInput;
